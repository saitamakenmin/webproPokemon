import * as React from 'react';

export default function Search() {
  return (
    <div style={styles.divStyle}>
      {/* ここに必要なコンテンツを追加 */}
    </div>
  );
}

const styles = {
  divStyle: {
    textAlign: "center",
    marginTop: 0,
    marginLeft: 500,
    marginRight: 500,
  }
}
